# fearlessfara/tap

Homebrew tap for [Envie](https://github.com/fearlessfara/envie).

```bash
brew install fearlessfara/tap/envie
```

Formula versions and checksums are updated automatically when a `v*` tag is pushed to `fearlessfara/envie`. The formula is not installable until the first GitHub Release exists.
